temporary readme.

create .venv, install all deps, source it
add orch dir to airflow path
airflow standalone

for some reason duckdb bin doesnt belong to .venv dir but still isolated maybe?

work on task granularity rather than monolithic run
